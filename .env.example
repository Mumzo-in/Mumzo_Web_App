# Single source of truth for the whole monorepo.
#
# Every workspace reads this one file:
#   - server / scripts  → dotenv, via packages/env (walks up to the repo root)
#   - vite apps         → envDir points here; only VITE_* reach the browser
#   - drizzle-kit       → packages/db/drizzle.config.ts
#   - docker compose    → infra/docker-compose.yml
#
# Gitignored. Copy .env.example to create it.

# --- Database ---------------------------------------------------------------
# Host is `localhost` from the host machine, `postgres` inside compose.
DATABASE_URL=postgresql://postgres:password@localhost:5432/mumzo

DATABASE_POOL_MAX=10
DATABASE_IDLE_TIMEOUT=30
DATABASE_CONNECT_TIMEOUT=10
DATABASE_SSL=false

# --- Postgres container -----------------------------------------------------
# NOTE: docker compose interpolates ${POSTGRES_*} relative to infra/, so it
# reads infra/.env — NOT this file. These are kept here only so the intended
# values live in one place; change infra/.env to actually affect the container,
# and keep both consistent with DATABASE_URL above.
POSTGRES_DB=mumzo
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_PORT=5432

# --- Server -----------------------------------------------------------------
PORT=3000
NODE_ENV=development

# --- Auth -------------------------------------------------------------------
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=replace-me-with-a-32-char-minimum-random-secret
BETTER_AUTH_URL=http://localhost:3000

# Comma-separated. Storefront (3001) and admin (3002) are separate origins.
CORS_ORIGIN=http://localhost:3001,http://localhost:3002

# "111111" verifies for any phone number when true. Local/staging only — never in production.
OTP_BYPASS=false

# --- Redis / notifications --------------------------------------------------
REDIS_URL=redis://localhost:6379
REDIS_PORT=6379

# Firebase service account (Project settings -> Service accounts -> Generate key)
FCM_PROJECT_ID=
FCM_CLIENT_EMAIL=
# Keep the \n literal — dotenv can't parse a real multi-line PEM value.
FCM_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Web Push VAPID key pair — generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:support@mumzo.in

# --- Frontend ---------------------------------------------------------------
# Only VITE_* variables are exposed to browser bundles. Never put a secret
# behind this prefix — it ships to every visitor.
# Used directly as the API origin — no hostname-based fallback. Set this to
# the real API for every deploy (including beta/staging), e.g.
# https://api.mumzo.in in production.
VITE_SERVER_URL=http://localhost:3000

# --- Cloudflare R2 -----------------------------------------------------------
# Account ID is in Cloudflare dashboard → R2 → Overview
R2_ENDPOINT=https://<ACCOUNT_ID>.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_PUBLIC_BUCKET=mumzo-public
R2_PRIVATE_BUCKET=mumzo-private
# Use the r2.dev URL from the bucket's Public Access settings page
R2_PUBLIC_URL=https://pub-<HASH>.r2.dev

# --- Upload processing -------------------------------------------------------
R2_MAX_UPLOAD_MB=20
R2_RESIZE_THRESHOLD_MB=1
R2_ENABLE_CDN_RESIZE=false       # flip to true once Image Resizing Worker is live
R2_IMAGE_MAX_DIMENSION=2048
R2_IMAGE_QUALITY=82
