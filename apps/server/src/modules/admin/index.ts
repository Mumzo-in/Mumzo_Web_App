/**
 * Admin module — everything the superadmin panel talks to.
 *
 * Versions live in their own folders (`./v1`, later `./v2`) and the URL
 * prefix is applied by `router.ts`.
 */

export { default as v1 } from "./v1";
